<RelativeLayout
    android:id="@+id/viewerLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context="jahirfiquitiva.apps.iconshowcase.activities.ViewerActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@color/primary"
        android:minHeight="?actionBarSize"
        android:transitionName="actionBar"
        app:contentInsetStart="@dimen/toolbar_contentInset"
        app:elevation="0dp"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        tools:ignore="UnusedAttribute"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar">

        <jahirfiquitiva.apps.iconshowcase.views.TouchImageView
            android:id="@+id/big_wallpaper"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"/>

        <ProgressBar
            android:id="@+id/progress"
            style="?progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:clickable="false"
            android:focusable="false"
            android:gravity="center"/>

    </FrameLayout>

    <com.github.clans.fab.FloatingActionMenu
        android:id="@+id/wallsFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_margin="16dp"
        app:menu_colorNormal="@android:color/white"
        app:menu_colorPressed="@android:color/white"
        app:menu_colorRipple="@color/semitransparent_black"
        app:menu_fab_size="normal"
        app:menu_icon="@drawable/ic_wallpapers_fab"
        app:menu_labels_colorNormal="@android:color/white"
        app:menu_labels_colorPressed="@android:color/white"
        app:menu_labels_colorRipple="@color/semitransparent_black"
        app:menu_labels_padding="8dp"
        app:menu_labels_textColor="@color/card_dark_background"
        app:menu_showShadow="true">

        <com.github.clans.fab.FloatingActionButton
            android:id="@+id/setWall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_check_dark"
            app:fab_colorNormal="@android:color/white"
            app:fab_colorPressed="@android:color/white"
            app:fab_colorRipple="@color/semitransparent_black"
            app:fab_label="@string/apply"
            app:fab_size="mini"/>

        <com.github.clans.fab.FloatingActionButton
            android:id="@+id/saveWall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_save_dark"
            app:fab_colorNormal="@android:color/white"
            app:fab_colorPressed="@android:color/white"
            app:fab_colorRipple="@color/semitransparent_black"
            app:fab_label="@string/save"
            app:fab_size="mini"/>

    </com.github.clans.fab.FloatingActionMenu>

</RelativeLayout>
